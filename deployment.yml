kind: Deployment
apiVersion: apps/v1
metadata:
  name: opl-webhook-handler
  namespace: hive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opl-webhook-handler
  template:
    metadata:
      labels:
        app: opl-webhook-handler
    spec:
      serviceAccountName: rhecoeng
      volumes:
        - name: kubeconfig
          secret:
            secretName: kubeconfig
      containers:
        - name: opl-webhook-handler
          command:
            - opl-webhook-handler
          env:
            - name: GITHUB_SECRET
              valueFrom:
                secretKeyRef:
                  name: github
                  key: hmac
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github
                  key: token
            - name: OPENSHIFT_KUBECONFIG
              value: /etc/.kube/config
          ports:
            - containerPort: 3000
              protocol: TCP
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: kubeconfig
              readOnly: true
              mountPath: /etc/.kube
          image: quay.io/opdev/opl-webhook-handler@sha256:c4cb3bf0483218dac24fd8d8c0e9fba988d260205db737250cd6ae690b0153f9
